{
  "name": "toktickit-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Root-level Playwright E2E harness for TokTickIT (client/server are separate npm packages; this is only for e2e/).",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
